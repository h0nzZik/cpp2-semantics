foo: () -> int = {
    return 3;
}

/*@ execution-claim [name]:
     ```k
     <k> startExec() ~> callFunction(String2Id("foo")) ~> readValue(lookupObjectID(unnamedReturnVariable())) => intValue(?_I:Int) ...</k>
     ```
@*/

bar: () -> int = {
    return 3;
}