foo: () -> int = {
    return 3;
}

/*@ execution-claim [name]:
     ```k
     <k> callFunction(String2Id("foo")) ~> readValue(unnamedReturnVariable()) => intValue(?_I:Int) ...</k>
     ```
@*/

bar: () -> int = {
    return 3;
}