#!/usr/bin/env bash

set -ue pipefail

build_dir="$(pwd)/.build"

error() { echo "$@"; exit 1 ; }

check() { diff -U3 "$@" ; }

make -s -C src || error "Build failed."
PATH="${build_dir}/bin:$PATH"
make -C tests
echo "Passed."
