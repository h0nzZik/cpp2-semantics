#!/usr/bin/env bash

set -ue pipefail

error() { echo "$@"; exit 1 ; }

make -C src || error "Build failed."
PATH="$(pwd)/.build/bin:$PATH"
kcpp2 --stop-after-disambiguation tests/main-return-42.cpp2
echo "Passed."
